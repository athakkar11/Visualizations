---
title: "AnshitaThakkar_Individual Visual Analytics Assignment2.R"
format: html
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
1 + 1
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).

Q1_1. Assign numeric value 4 to A and compute (A -- 4*√𝐴)/(A+ 5*√𝐴).

```{r}
A <- 4
result <- (A - 4*sqrt(A))/ (A + 5 * sqrt(A))
result

```

Q1_2. Create sequence of numbers that count up by 4 from 0 to 48

```{r}
seq(0, 48, by = 4)
```

Q1_3. Use R to compute followings (27 = 2\^7 in R) 27

```{r}
result <- 2^7
print(result)
```

```{r}
(2^7) / (2^7 - 1)
(1-(1/2^7))^-1
exp(0.4)
1+0.3+(0.3^2)+(0.3^3)
(1+0.3+0.3^2+0.3^3+0.3^5)/5

```

Q1_4. Create a matrix that consists of eight country names (number of rows = 4, number of columns = 2 and convert the matrix into data frame. Make sure if the matrix successfully changes into dataframe.

```{r}
matrix <- matrix(c("United States", "New Zealand", "India", "Japan", "Australia", "United Kingdom","France", "Spain"), nrow = 4, ncol = 2)
print(matrix)

countriesdf <- as.data.frame(matrix)
countriesdf
```

Q1_5. Create two vectors that consist of four integer values for each and multiply the vectors. Example code: a \<- c(2L, 4L, 8L, 10L) #have to have 'L' after each number to indicate an integer value.

```{r}
b <- c(1L, 2L, 3L, 4L)
c <- c(3L, 6L, 9L, 12L)

b*c

is.vector(b)
is.vector (c)

```

Q1_6. Create a data frame (named friend) that includes following variables. \# name consists of four friends' names\
\# age consists of their ages \# momAge consists of their mothers' ages (You can make up the age) \# dadAge consists of their fathers' ages (You can make up the age)

```{r}
name <- c("Aashay", "Tiffany", "Simone", "Deeva") 
age <- c(31, 30, 27, 25)
momAge <- c(51, 50, 57, 55)
dadAge <- c(55, 52, 62, 53)
name; age; momAge; dadAge


friend <- cbind(name, age, momAge, dadAge)
friend

frienddf <- as.data.frame(friend)
frienddf

```

Q1_7. Create an additional column in the data frame (friend) that results in the total ages for the three persons in each family (= friend + mother + father). Example code: \# Convert character value to numeric to add frienddf$age <- as.numeric(as.character(frienddf$age)) frienddf$momAge <- as.numeric(as.character(frienddf$momAge)) frienddf$dadAge <- as.numeric(as.character(frienddf$dadAge)) \# Add a column for total age frienddf$totalAge <- frienddf$age + frienddf$momAge + frienddf$dadAge

```{r}
frienddf$age <- as.numeric(as.character(frienddf$age))
frienddf$momAge <- as.numeric(as.character(frienddf$momAge))
frienddf$dadAge <- as.numeric(as.character(frienddf$dadAge))
frienddf$totalAge <- frienddf$age + frienddf$momAge + frienddf$dadAge

frienddf$totalAge
frienddf
```

Q1_8. Install and load R packages 'ggplot2' and 'psych'.

```{r}
install.packages("ggplot2")
library(ggplot2)

install.packages("psych")
library(psych)
```

Q1_9. Download 'demand1.csv' data file from Pace Class Portal and save it in a desired folder. Import the data file into R using syntax in R script. Example code: demand1 \<- read.csv(file = "\~/demand1.csv", sep = ",", header = T) 2

```{r}
demand1 <- read.csv(file = "/Users/anshitathakkar/Documents/Visual analytics/Assignment 2/demand1.csv", sep = ",", header = T)
demand1
```

Q1_10. Download 'adSpend.csv' data from Pace Class Portal and save it in a desired folder. Import the data file into R using syntax in R script and cbind it with 'demand1.csv' and name the new data frame 'demand2.csv'. Export the 'demand2.csv' to a desired folder. Example code: demand2 \<- cbind(demand1, adSpend) write.csv(demand2, "\~/demand2.csv", sep=",")

```{r}
adSpend <- read.csv(file = "/Users/anshitathakkar/Documents/Visual analytics/Assignment 2/adSpend.csv", sep = ",", header = T)
adSpend
```

```{r}
demand2 <- cbind(demand1, adSpend)
demand2
```

```{r}
demand2 = write.csv(demand2, "/Users/anshitathakkar/Documents/Visual analytics/Assignment 2/demand2.csv", sep=",")
demand2 = read.csv(file = "/Users/anshitathakkar/Documents/Visual analytics/Assignment 2/demand2.csv", sep = ",", header = T)
print(demand2)
```
